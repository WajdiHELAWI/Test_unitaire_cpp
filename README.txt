This is ana exemple of how to do an unite test using google test (gtest)

To compile this project:
```bash
$ cmd 1 : cmake --version 
$ cmd 2 : mkdir build 
$ cmd 3 : cd build/
$ cmd 4 : cmake .. 
$ cmd 5 : make 
$ cmd 6 : ./mainApp
$ cmd 7 : ls _deps/ 
$ cmd 8 : ./testRunner 
```