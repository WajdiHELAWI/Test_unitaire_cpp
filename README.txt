This is ana exemple of how to do an unite test using google test (gtest)

To compile this project:
```bash
cmake --version 
mkdir build 
cd build/
cmake .. 
make 
./mainApp
ls _deps/ 
./testRunner 
```